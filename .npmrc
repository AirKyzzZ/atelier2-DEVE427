# Flatten node_modules so tools that don't understand pnpm's
# isolated symlink layout (e.g. eslint-plugin-tailwindcss looking
# up the `tailwindcss` package) can still resolve their peers.
# This matches the layout pnpm < 7 used by default.
shamefully-hoist=true
node-linker=hoisted
